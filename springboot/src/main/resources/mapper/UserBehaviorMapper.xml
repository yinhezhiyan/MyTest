<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserBehaviorMapper">
    <insert id="insert" parameterType="com.example.entity.UserBehavior" useGeneratedKeys="true" keyProperty="id">
        insert into user_behavior(user_id, exercise_id, action_type, is_correct, score)
        values(#{userId}, #{exerciseId}, #{actionType}, #{isCorrect}, #{score})
    </insert>
</mapper>
